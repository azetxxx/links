# TUNNEL: https://pamela-exciting-compaq-kind.trycloudflare.com (regenerate via npm run tunnel:trycloudflare)
# Web app (HTML/JS/CSS) is served directly by Netlify from this public/ dir.
# Only the bridge API endpoints redirect to the tunnel.

# Bridge URL discovery: app fetches /health, follows this 302, reads response.url → current tunnel origin.
/health  https://pamela-exciting-compaq-kind.trycloudflare.com/health  302

# Short link resolution (system camera QR scan → tunnel resolves slug → redirects to s.az-studio.pro/?p=…).
/s/*  https://pamela-exciting-compaq-kind.trycloudflare.com/s/:splat  302
